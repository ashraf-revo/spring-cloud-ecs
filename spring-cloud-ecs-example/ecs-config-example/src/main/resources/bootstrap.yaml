spring:
  application:
    name: ecs-config-example
  cloud:
    ecs:
      config:
        region: "us-east-1"
        enabled: true
        paths:
          - "s3://replox/ipmcg.yaml"
cloud:
  aws:
    region:
      static: us-east-1
management:
  endpoints:
    web:
      exposure:
        include: "*"
